# Secret for GitHub token (template)
# Replace YOUR_GITHUB_TOKEN_BASE64 with your actual token encoded in base64
# To encode: echo -n "your-github-token" | base64
apiVersion: v1
kind: Secret
metadata:
  name: github-token
  namespace: cubrid-testing
type: Opaque
data:
  token: YOUR_GITHUB_TOKEN_BASE64
